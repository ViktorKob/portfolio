####################
# Config server settings:
####################

spring:
   profiles:
      active: development
   cloud:
      config:
         server:
            git:
               uri: https://github.com/ViktorKob/portfolio-config.git
               search-paths: '{profile}/{application}' 
